[Unit]
Description=PiRogue admin web

[Service]
ExecStart=/usr/sbin/pirogue-admin-web
Restart=on-failure
RestartSec=2
Type=notify

[Install]
# Tell systemd to automatically start this service when the system boots
# (assuming the service is enabled)
WantedBy=default.target